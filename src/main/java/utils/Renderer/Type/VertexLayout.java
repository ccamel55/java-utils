package utils.Renderer.Type;

public class VertexLayout {
    public int iCount;
    public int iOffset;

    public VertexLayout(int count, int offset) {
        iCount = count;
        iOffset = offset;
    }
}
